<Window x:Class="PLWPF.Test_Registar_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         WindowStartupLocation="CenterScreen"
        xmlns:local="clr-namespace:PLWPF"
        mc:Ignorable="d"
        Title="Test_Registration_Window" MinHeight="500" MinWidth="500" SizeToContent="WidthAndHeight">
    <Window.Resources>
        <Style x:Key="MyStyle1" TargetType="Control">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="20,10,0,10"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="28"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="20,10"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions >
            <RowDefinition  Height="0.5*"/>
            <RowDefinition  Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition  Height="*"/>
        </Grid.RowDefinitions >
        <Grid>
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>
            <Button Content="יציאה" Margin="20,5,5,5"/>
            <Button Content="תפריט ראשי" Grid.Column="1" Margin="5"/>

        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions >
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions >
            <TextBox x:Name="testAdressTextBox" Style="{StaticResource MyStyle1}">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <DatePicker x:Name="testDatePicker" Grid.Row="1" Style="{StaticResource MyStyle1}"/>
            <ComboBox x:Name="testHourComboBox" Grid.Row="2" Style="{StaticResource MyStyle1}"/>
            <Label x:Name="testAdressLable" Content=":כתובת" Grid.Column="1"/>
            <Label x:Name="testDateLable" Content=":תאריך"  Grid.Row="1" Grid.Column="1"/>
            <Label x:Name="testHourLable" Content=":שעה" Grid.Row="2" Grid.Column="1"/>
        </Grid>
        <Label Content="הרשם לטסט" Grid.Row="1" HorizontalAlignment="Center"/>
        <Button Content="אישור" Grid.Row="3" Margin="15" FontSize="36" FontWeight="Heavy"/>
    </Grid>
</Window>
